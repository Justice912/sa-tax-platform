---
phase: 06-logbook-ui-import-wizard-performance
plan: 06
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]
files_modified:
  - src/components/individual-tax/tax-tools/shared.tsx
  - src/app/(protected)/individual-tax/tools/page.tsx
  - src/components/individual-tax/tax-tools.tsx
  - src/components/individual-tax/tax-tools/travel-logbook-tab.tsx
  - src/components/individual-tax/tax-tools/travel-logbook-tab.test.tsx
  - src/components/individual-tax/tax-tools/render-isolation.test.tsx
autonomous: true
requirements: [LOG-06, PERF-02, PERF-03]
must_haves:
  truths:
    - "User selects a client in the travel tab and sees that client's persisted logbook (vehicle, odometers, trips) for the selected tax year"
    - "User can add/edit/delete a trip using the real domain schema (direct business km + optional odometer), and the deemed & actual figures update to real recomputed values"
    - "User can import a CSV/XLSX via the wizard and the committed trips persist and appear in the virtualized table"
    - "User can export the logbook as CSV and open the printable SARS audit summary"
    - "The travel deduction published to the Dashboard is the real claimed deduction"
    - "Typing in another calculator does not re-render the travel tab (isolation preserved)"
  artifacts:
    - path: "src/components/individual-tax/tax-tools/travel-logbook-tab.tsx"
      provides: "Container: client/year resolution, logbook load, trip CRUD modal, wired TripTable + CostMethodPanel + LogbookImportWizard, CSV/print, summary publish"
      min_lines: 150
      contains: "getLogbookForClientAction"
    - path: "src/app/(protected)/individual-tax/tools/page.tsx"
      provides: "Async page fetching individual clients and passing them to TaxTools"
      contains: "listClients"
    - path: "src/components/individual-tax/tax-tools/travel-logbook-tab.test.tsx"
      provides: "Integration proof: load -> add trip -> real result update -> import -> export"
  key_links:
    - from: "src/components/individual-tax/tax-tools/travel-logbook-tab.tsx"
      to: "src/modules/logbook/actions.ts"
      via: "get/add/update/delete/import/setCostMethod/setActualExpenses actions, merge { record, travelResult }"
      pattern: "Action\\("
    - from: "src/components/individual-tax/tax-tools/travel-logbook-tab.tsx"
      to: "summary-context (useSummaryWriter)"
      via: "publish travelResult.claimedDeduction as travelDeduction"
      pattern: "setSummaryValue\\(\"travelDeduction\""
    - from: "src/app/(protected)/individual-tax/tools/page.tsx"
      to: "src/components/individual-tax/tax-tools.tsx"
      via: "<TaxTools clients={clients} />"
      pattern: "<TaxTools"
---

<objective>
The keystone: assemble the three previously-isolated systems (persisted logbook domain, Phase 4 import pipeline, Phase 5 decomposed UI) into ONE working end-to-end travel-logbook feature. Rewrite `travel-logbook-tab.tsx` from a 964-line local-state prototype into a thin container that resolves a client + tax year, loads the REAL persisted logbook via Server Actions, and orchestrates the sub-components built in 06-02..06-05. Replace the `Trip`/`UploadData` prototype types (schema mismatch — Pitfall 1) with the real `LogbookTripRecord` domain shape.

Purpose: This is where "real, not stubbed" becomes true (success criteria 1-4). Every mutation goes through an action that returns `{ record, travelResult }` merged into local state — no client-side cost math, no revalidatePath-per-edit (Pitfalls 2 & 3). The Dashboard summary now publishes the real claimed deduction.

Output: rewritten container + async page + threaded shell prop + type cleanup + integration test + updated isolation test.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-logbook-ui-import-wizard-performance/06-RESEARCH.md
@src/components/individual-tax/tax-tools/travel-logbook-tab.tsx
@src/components/individual-tax/tax-tools/shared.tsx
@src/components/individual-tax/tax-tools.tsx
@src/app/(protected)/individual-tax/tools/page.tsx
@src/app/(protected)/individual-tax/new/page.tsx

<interfaces>
Server Actions (06-01) — import directly into this "use client" component:
```typescript
type LogbookState = { record: LogbookRecord; travelResult: LogbookTravelResult };
getLogbookForClientAction(clientId, assessmentYear): Promise<LogbookState | null>
createLogbookAction(input: CreateLogbookForClientInput): Promise<LogbookState>
addTripAction(logbookId, trip): Promise<LogbookState>
updateTripAction(logbookId, tripId, patch): Promise<LogbookState>
deleteTripAction(logbookId, tripId): Promise<LogbookState>
importTripsAction(logbookId, trips, source): Promise<LogbookState>
setCostMethodAction(logbookId, method): Promise<LogbookState>
setActualExpensesAction(logbookId, expenses): Promise<LogbookState>
updateOdometersAction(logbookId, odometers): Promise<LogbookState>
getLogbookCsvAction(logbookId): Promise<string>
```
Sub-components (06-02/03/04) — import and wire:
```typescript
TripTable({ trips, onEditTrip, onDeleteTrip, busy? })
LogbookImportWizard({ logbook: { openingOdometer, closingOdometer, existingTrips }, onCommit, onClose })
CostMethodPanel({ record, travelResult, onElectMethod, onSaveExpenses, busy? })
```
Print route (06-05): link to `/reports/logbook/${record.id}/print`.
Clients: `import type { ClientRecord } from "@/modules/shared/types"` (fields: id, displayName, clientType, taxReferenceNumber). `listClients` from "@/modules/clients/client-service" (server-only — call in the async page, pass down).
Context: `useRulePack()` -> `assessmentYear` (drives the loaded logbook year); `useSummaryWriter()` -> `setSummaryValue("travelDeduction", n)`.
</interfaces>

<constraints>
- BACKWARD COMPAT: existing tests render `<TravelLogbookTab />` and `<TaxTools />` with NO props (render-isolation.test.tsx:101,360 etc.). Make the new `clients` prop OPTIONAL (default `[]`). With no clients, render a "no individual clients" empty state and DO NOT auto-fire any Server Action on mount — the load action fires only after a client is selected. This keeps the shell and isolation tests renderable without a server round trip.
- The trip form uses the REAL schema ONLY: date, businessKm (direct number entry), fromLocation, toLocation, reason, optional odometerStart/odometerEnd. NO tripType / mixedSplit / privateKm anywhere (any attempt fails tripInputSchema — that failure is a canary, not a bug to patch, per Pitfall 1).
- Keep truncation on the reason column; keep useRulePack + useSummaryWriter wiring alive (research point 7).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumbing — type cleanup, async page, shell prop threading</name>
  <files>src/components/individual-tax/tax-tools/shared.tsx, src/app/(protected)/individual-tax/tools/page.tsx, src/components/individual-tax/tax-tools.tsx</files>
  <action>
    - shared.tsx: DELETE the `Trip` and `UploadData` interfaces (verified only travel-logbook-tab.tsx imported them; it is being rewritten in Task 2). Leave fmt/fmtKm/pct/MONTHS/TabKey/StatCard/ResultCard/Highlight/Field/inputCls/selectCls untouched.
    - tools/page.tsx: make the default export `async`. Fetch `const clients = (await listClients()).filter(c => c.clientType === "INDIVIDUAL").sort((a,b) => a.displayName.localeCompare(b.displayName))` (mirror new/page.tsx:19-22). Render `<TaxTools clients={clients} />`. Keep the existing header/back-link markup.
    - tax-tools.tsx: give `TaxTools` an optional prop `{ clients = [] }: { clients?: ClientRecord[] }` and thread it: `<TravelLogbookTab clients={clients} />`. Leave RulePackProvider/SummaryProvider/TaxToolsInner and the other 7 tabs untouched (they remain client-agnostic sandboxes — do NOT widen their props).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Trip/UploadData removed from shared.tsx; tools/page.tsx is async and passes individual clients; TaxTools threads an optional clients prop to TravelLogbookTab only; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite travel-logbook-tab.tsx as the wiring container</name>
  <files>src/components/individual-tax/tax-tools/travel-logbook-tab.tsx</files>
  <action>
    Rewrite the component with signature `export function TravelLogbookTab({ clients = [] }: { clients?: ClientRecord[] })`. Delete ALL of: the old Trip[] local state, allocTrip/newTrip/changeTripType, handleFile/processImport/finaliseImport/bulkClassify (the naive parser), exportCSV, the client-side getDeemedRate/deemed math, and the tripType/mixedSplit UI. Build:
    - Client selector `<select>` from `clients` (disabled/empty-state when none). `assessmentYear` from `useRulePack()`.
    - State: `state: LogbookState | null`, `loading`, `busy`, plus a `toast` (reuse the existing local toast pattern). On (selectedClientId, assessmentYear) change with a client selected: `getLogbookForClientAction(clientId, year)` -> setState (null => show empty state).
    - Empty state when no logbook for the client+year: a minimal create form (vehicle make/model/registrationNumber/costPrice, openingOdometer, optional closingOdometer) calling `createLogbookAction` and setting state from the result.
    - When a logbook is loaded: render an odometer summary + editor (updateOdometersAction), the `CostMethodPanel` (onElectMethod -> setCostMethodAction, onSaveExpenses -> setActualExpensesAction), the `TripTable` (onEditTrip/onDeleteTrip), a "+ New Trip" button opening the trip form modal (real schema; add -> addTripAction, edit -> updateTripAction), and a "+ Import" button opening `LogbookImportWizard` (onCommit -> importTripsAction(record.id, validTrips, source)).
    - A generic `applyAction` helper: set busy, await the action, merge the returned `{ record, travelResult }` into state, catch errors -> toast the message, finally clear busy. Every mutation goes through it so a single edit never refetches the whole page (PERF-03) and never re-derives cost math client-side (Pitfall 3).
    - Toolbar: "Export CSV" button -> `getLogbookCsvAction(record.id)` -> Blob download (reuse the a[download] pattern). "Print / audit summary" -> a link/anchor to `/reports/logbook/${record.id}/print` (target _blank).
    - Publish summary: `useEffect(() => setSummaryValue("travelDeduction", state?.travelResult.claimedDeduction ?? 0), [state, setSummaryValue])`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Container loads a real logbook via actions, supports create/odometer/trip-CRUD/import/cost-method/expenses all through actions with { record, travelResult } merge, exports CSV + links to print, and publishes the real claimed deduction; no naive parser, no client cost math, no tripType; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test + preserve isolation; full suite green</name>
  <files>src/components/individual-tax/tax-tools/travel-logbook-tab.test.tsx, src/components/individual-tax/tax-tools/render-isolation.test.tsx</files>
  <action>
    New travel-logbook-tab.test.tsx: `vi.mock("@/modules/logbook/actions")` returning canned LogbookState objects (so no node:fs/worker in jsdom); apply `mockScrollElementSize` for the virtualized table. Assert the end-to-end wiring (phase success criteria):
    1. With a `clients` prop and a mocked `getLogbookForClientAction` returning a logbook, selecting the client renders its trips (in TripTable) and the CostMethodPanel's deemed & actual figures.
    2. Adding a trip via the modal (real fields: date, business km, from, to, reason) calls `addTripAction` and, on the mock returning an updated state with a higher claimedDeduction, the displayed claimed deduction updates (real result, not stubbed — criterion 4).
    3. Committing an import via the wizard calls `importTripsAction` with the valid trips and source; the returned trips appear.
    4. The published summary value equals the mocked `travelResult.claimedDeduction` (render inside a summary provider + a probe reading `useSummary`).
    5. Export CSV button triggers `getLogbookCsvAction`.
    Update render-isolation.test.tsx (the `Travel/Medical render isolation` block, lines ~352-455): the old travel assertions type into the **"Determined Value (R)"** input and drive `newTrip`/`tripType`, all of which THIS plan removes — those tests will no longer find their targets, so they must be migrated, not left to break. Preserve the must_have isolation invariant with a REAL state change under the new UI (do NOT make it vacuous against the empty state), as follows:
      - **Primary (the must_have direction — "typing in another calculator does not re-render the travel tab"):** this needs NO travel input. Render `<TravelLogbookTab />` with NO `clients` prop (empty state; per the Task-2 constraint it fires no Server Action on mount, so the tree is stable) inside a `<Profiler id="travel">`, alongside `<MedicalTab />` in a `<Profiler id="medical">`. After mount, `mockClear()` both `onRender` spies, then `user.type` into MedicalTab's real input (`/monthly medical aid contribution \(r\)/i`). Assert `onRenderMedical` fired (a genuine sibling state change occurred — non-vacuous) and `onRenderTravel` was NOT called. This is the must_have, verifiably tested.
      - **Reverse direction (a state change originating INSIDE the rewritten travel tab):** the empty state has no input, so reach a real one by `vi.mock("@/modules/logbook/actions")` — reuse the EXACT canned-`LogbookState` mock from the new travel-logbook-tab.test.tsx (so no `node:fs`/worker runs in jsdom) — and render the travel tab with a one-element `clients` prop plus `mockScrollElementSize`. `user.selectOptions` the client so `getLogbookForClientAction` (mocked) loads the canned logbook, exposing a real input (the odometer editor field, or open the "+ New Trip" modal and use its Business KM field). `mockClear` both spies, type into that travel input, and assert `onRenderTravel` fired while `onRenderMedical` was NOT called. (If keeping this direction is more churn than its worth, the primary direction above already satisfies the must_have; but do not leave a dead test asserting against the removed determined-value field.)
      - Keep every existing `<TaxTools />` and `<TravelLogbookTab />` NO-prop render in the file working (clients defaults to `[]` -> empty state). The old "still publishes Travel deduction to the Dashboard" and "keeps in-progress vehicle-value input across tab switch" travel tests (lines ~389-455) also depend on the removed prototype UI — the deduction-publish behavior is now covered by this plan's integration test (steps 2 & 4), so remove/migrate those two rather than leave them referencing `newTrip`/`determined value`.
    Then run the full tax-tools test directory to prove no Phase-5 regression.
  </action>
  <verify>
    <automated>npx vitest run src/components/individual-tax/tax-tools</automated>
  </verify>
  <done>Integration test proves load -> add-trip-updates-real-result -> import -> summary-publish -> CSV export; render-isolation suite (including the updated travel assertions and all <TaxTools /> renders) passes with no regressions.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/individual-tax/tax-tools src/modules/logbook` passes (integration + isolation + actions).
- `npm run build` completes without "Failed to compile" (grep the full output — do NOT trust a piped `| tail` exit code; do NOT use `next build --webpack`, which fails on a pre-existing unrelated Estates route error per STATE.md).
- Grep confirms travel-logbook-tab.tsx contains no `tripType`/`mixedSplit`/`getDeemedRate`/`FileReader` and no per-km rate arithmetic.
</verification>

<success_criteria>
The travel logbook tab loads a real per-client, per-year persisted logbook; trip CRUD, import, cost-method election, and expense capture all round-trip through Server Actions returning real recomputed deemed/actual results; CSV export and the printable audit summary work; the Dashboard shows the real claimed deduction — the whole feature working end-to-end at scale (virtualized), with calculator isolation preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-logbook-ui-import-wizard-performance/06-06-SUMMARY.md` recording the final container structure, the clients-optional backward-compat decision, and any deviations. Then update ROADMAP.md Phase 6 status and run `roadmap update-plan-progress 6` (unpadded phase number).
</output>

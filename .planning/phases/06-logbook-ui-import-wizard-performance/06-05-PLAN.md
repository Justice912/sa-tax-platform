---
phase: 06-logbook-ui-import-wizard-performance
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reports/logbook-audit-summary.tsx
  - src/components/reports/logbook-audit-summary.test.tsx
  - src/app/reports/logbook/[logbookId]/print/page.tsx
autonomous: true
requirements: [LOG-06]
must_haves:
  truths:
    - "A practitioner can open a printable SARS-audit summary for a logbook at /reports/logbook/{logbookId}/print"
    - "The summary shows vehicle details, opening/closing odometers, total & business km, the full trip list, and the deemed/actual travel result"
    - "The page is print-styled (A4, @page margins)"
  artifacts:
    - path: "src/components/reports/logbook-audit-summary.tsx"
      provides: "Presentational printable audit summary over a LogbookAuditSummary"
      min_lines: 70
    - path: "src/app/reports/logbook/[logbookId]/print/page.tsx"
      provides: "Async route that fetches getLogbookAuditSummary and renders the summary with print CSS"
      contains: "getLogbookAuditSummary"
  key_links:
    - from: "src/app/reports/logbook/[logbookId]/print/page.tsx"
      to: "src/modules/logbook/service.ts"
      via: "getLogbookAuditSummary(logbookId)"
      pattern: "getLogbookAuditSummary"
    - from: "src/app/reports/logbook/[logbookId]/print/page.tsx"
      to: "src/components/reports/logbook-audit-summary.tsx"
      via: "renders <LogbookAuditSummaryView summary={...} />"
      pattern: "LogbookAuditSummaryView"
---

<objective>
Ship the printable SARS-audit summary (LOG-06's "printable summary" half — the CSV half already exists and gets a button in the container plan). `export.ts:buildLogbookAuditSummary` explicitly documented "the printable HTML/PDF rendering is explicitly Phase 6"; this delivers it, mirroring the existing individual-tax print-page pattern.

Purpose: `getLogbookAuditSummary(logbookId)` already assembles the exact data shape (vehicle, odometers, totals, trips, travelResult). The work is a thin async route + a presentational component, following `src/app/reports/individual-tax/[assessmentId]/print/page.tsx` (async server component + `@page` print CSS + a prop-driven report component like `IndividualTaxIta34`).

Output: `logbook-audit-summary.tsx` (prop-driven, testable), the print route, and a component test.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-logbook-ui-import-wizard-performance/06-RESEARCH.md
@src/app/reports/individual-tax/[assessmentId]/print/page.tsx
@src/modules/logbook/export.ts

<interfaces>
From src/modules/logbook/service.ts (server-only, called directly by the async route):
```typescript
getLogbookAuditSummary(logbookId: string): Promise<LogbookAuditSummary>
```
From src/modules/logbook/types.ts:
```typescript
interface LogbookAuditSummary {
  clientId: string; assessmentYear: number;
  vehicle: { make: string; model: string; registrationNumber: string; costPrice: number; acquisitionDate?: string | null };
  openingOdometer: number; closingOdometer: number | null;
  totalKilometres: number | null; totalBusinessKm: number; tripCount: number;
  trips: LogbookTripRecord[];                 // date, businessKm, fromLocation, toLocation, reason, odometer*
  travelResult: LogbookTravelResult;          // deemedCostDeduction, actualCostDeduction, costMethod, claimedDeduction, recommendedMethod, warnings
  generatedAt: string;
}
```
Route param convention (Next 16): `params: Promise<{ logbookId: string }>` — `await params` before use (see the individual-tax print page).
`notFound()` from "next/navigation" when the summary throws / logbook missing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the presentational audit-summary component</name>
  <files>src/components/reports/logbook-audit-summary.tsx</files>
  <action>
    Create `LogbookAuditSummaryView({ summary }: { summary: LogbookAuditSummary })` rendering a SARS-audit-suitable printable layout:
    - Header: "Travel Logbook — SARS Audit Summary", assessment year, generatedAt.
    - Vehicle block: make/model, registration, cost price (R), acquisition date.
    - Odometer block: opening, closing (or "not recorded"), total km, total business km, trip count.
    - Trip table: a plain full (NON-virtualized — this is a print document, all rows must be present) `<table>` of every trip: date, from -> to, reason, business km, odometer start/end. Keep it a real table so print pagination works.
    - Cost result block: deemed vs actual figures, elected method, claimed deduction, and any warnings.
    Use plain semantic markup + Tailwind (this is presentational; do not fetch anything, do not import service.ts). Reuse currency formatting consistent with the app (a local `R `-prefixed toLocaleString("en-ZA") formatter is fine — keep it inline, this component lives under components/reports not tax-tools).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>LogbookAuditSummaryView renders all summary sections including the full (non-virtualized) trip table; no data fetching; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the print route and test the component</name>
  <files>src/app/reports/logbook/[logbookId]/print/page.tsx, src/components/reports/logbook-audit-summary.test.tsx</files>
  <action>
    Route: create an async server component mirroring src/app/reports/individual-tax/[assessmentId]/print/page.tsx — `await params` to get logbookId, `const summary = await getLogbookAuditSummary(logbookId)` inside try/catch; on throw call `notFound()`. Render the same `<style>{@page A4 + margins + print-color-adjust}</style>` block, then `<LogbookAuditSummaryView summary={summary} />`.

    Test (component-level, since testing async server components in vitest is awkward — test the presentational component with a fixture, the dominant precedent for IndividualTaxIta34): with an inline `LogbookAuditSummary` fixture containing 2-3 trips and a complete travelResult, render `<LogbookAuditSummaryView summary={fixture} />` and assert: vehicle registration, both odometer readings, total business km, every trip's date/route, and both the deemed and actual figures and the claimed deduction appear. Use the raw-normalizer trick for exact currency strings if asserting formatted amounts.
  </action>
  <verify>
    <automated>npx vitest run src/components/reports/logbook-audit-summary.test.tsx</automated>
  </verify>
  <done>The /reports/logbook/[logbookId]/print route fetches getLogbookAuditSummary and renders the print-styled summary; the component test proves all sections render from a fixture.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/reports/logbook-audit-summary.test.tsx` passes.
- Route file exists at src/app/reports/logbook/[logbookId]/print/page.tsx and imports getLogbookAuditSummary + LogbookAuditSummaryView.
- The print trip table is a full non-virtualized table (all trips present for audit).
</verification>

<success_criteria>
A practitioner can open a print-ready SARS audit summary for any logbook showing vehicle, odometers, every trip, and the deemed/actual result — the printable half of LOG-06.
</success_criteria>

<output>
After completion, create `.planning/phases/06-logbook-ui-import-wizard-performance/06-05-SUMMARY.md` recording the route path and the component prop contract so the container plan can link to it.
</output>

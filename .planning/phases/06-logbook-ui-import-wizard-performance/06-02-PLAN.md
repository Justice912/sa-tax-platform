---
phase: 06-logbook-ui-import-wizard-performance
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/individual-tax/tax-tools/trip-table.tsx
  - src/components/individual-tax/tax-tools/trip-table.test.tsx
autonomous: true
requirements: [PERF-02, PERF-03]
must_haves:
  truths:
    - "A trip table rendered with 10,000 trips shows only a bounded window of row nodes, not 10,000"
    - "The user can filter trips by month and the visible rows update"
    - "Editing/deleting a single trip goes through parent callbacks (the table does not re-derive cost math)"
  artifacts:
    - path: "src/components/individual-tax/tax-tools/trip-table.tsx"
      provides: "Headless-virtualized, month-filterable trip table over LogbookTripRecord[]"
      min_lines: 80
      contains: "useVirtualizer"
    - path: "src/components/individual-tax/tax-tools/trip-table.test.tsx"
      provides: "PERF-02 bounded-DOM proof at 10k rows + PERF-03 filter time-budget proof"
  key_links:
    - from: "src/components/individual-tax/tax-tools/trip-table.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer over the scroll container"
      pattern: "useVirtualizer"
    - from: "src/components/individual-tax/tax-tools/trip-table.test.tsx"
      to: "src/test/virtualization-test-utils.tsx"
      via: "mockScrollElementSize (recipe from 06-01)"
      pattern: "mockScrollElementSize"
---

<objective>
Build the persisted trip table as a self-contained, headless-virtualized component that stays responsive at 10,000+ trips (PERF-02, PERF-03). It renders the REAL domain shape (`LogbookTripRecord`: `businessKm` direct + optional odometer readings) — NOT the old Business/Private/Mixed classifier, which cannot round-trip through the domain model (research Pitfall 1).

Purpose: The current trip table is a plain `.map()` over an array (travel-logbook-tab.tsx:737-806) that mounts every row. This replaces it with `@tanstack/react-virtual` so only a bounded viewport window is ever in the DOM. It owns its own month filter + memoized derivations so the container stays thin.

Output: `trip-table.tsx` (pure: props + callbacks, no server actions, no cost math) and its test proving virtualization + filter throughput.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-logbook-ui-import-wizard-performance/06-RESEARCH.md
@src/components/individual-tax/tax-tools/shared.tsx
@src/components/individual-tax/tax-tools/travel-logbook-tab.tsx

<interfaces>
From src/modules/logbook/types.ts:
```typescript
interface LogbookTripRecord {
  id: string; date: string; businessKm: number;
  fromLocation: string; toLocation: string; reason: string;
  odometerStart?: number | null; odometerEnd?: number | null;
  createdAt: string; updatedAt: string;
}
```
From src/components/individual-tax/tax-tools/shared.tsx (reuse as-is, do NOT modify here):
```typescript
export const fmtKm: (n: number) => string;   // "1 234.5 km"
export const MONTHS: string[];                // ["Jan",...,"Dec"]
export const selectCls: string;
```
From src/test/virtualization-test-utils.tsx (created in 06-01):
```typescript
export function mockScrollElementSize(height?: number, itemHeight?: number): () => void;
```

CONTRACT this component must expose (the container in 06-06 depends on exactly this):
```typescript
interface TripTableProps {
  trips: LogbookTripRecord[];                       // full list; component owns filter + virtualization
  onEditTrip: (trip: LogbookTripRecord) => void;
  onDeleteTrip: (tripId: string) => void;
  busy?: boolean;                                   // disable row actions during a pending mutation
}
export function TripTable(props: TripTableProps): JSX.Element;
```
</interfaces>

<research_notes>
- Headless virtualization drops into existing `<table>` markup via a fixed-height scroll `div` (overflow-y:auto) wrapping absolutely-positioned rows keyed by trip.id (06-RESEARCH.md Pattern 1). Keep the "Purpose"/reason column truncated (`max-w-[160px] truncate`) so fixed `estimateSize` virtualization is valid — dynamic row height is explicitly out of scope (Open Q3).
- Give each rendered row a `data-virtual-row` attribute so tests can count bounded DOM nodes with the 06-01 recipe.
- React-19 caveat: if getVirtualItems() returns a frozen 0-length array, hold the virtualizer in a useRef and/or pass `useFlushSync: false` (recipe/decision recorded in 06-01-SUMMARY).
</research_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the virtualized, month-filterable trip table</name>
  <files>src/components/individual-tax/tax-tools/trip-table.tsx</files>
  <action>
    Create a `"use client"` `TripTable` component matching the `TripTableProps` contract above. It owns:
    - Local `filterMonth` state ("all" | 0..11) with a `<select>` (MONTHS) using `selectCls`.
    - A `useMemo` deriving `filteredTrips` = trips filtered by `new Date(t.date).getMonth()` then sorted date-ascending (mirror the existing memo at travel-logbook-tab.tsx:257-269).
    - A `useVirtualizer` over a `parentRef` scroll container (fixed height, e.g. 480px, `overflow-y:auto`), `count: filteredTrips.length`, `estimateSize: () => 40`, `overscan: 10`. Render only `virtualizer.getVirtualItems()`, each row absolutely positioned (`transform: translateY(vRow.start)`), keyed by `filteredTrips[vRow.index].id`, with a `data-virtual-row` attribute.
    - Columns matching the real schema: Date, Route (fromLocation -> toLocation), Business KM (`fmtKm(businessKm)`), Odometer (start/end or "—"), Reason (truncated), and an actions cell with Edit (calls `onEditTrip(trip)`) and Delete (calls `onDeleteTrip(trip.id)`) buttons, both disabled when `busy`.
    - An empty state ("No trips yet") when `filteredTrips.length === 0`.
    - A totals footer row showing `fmtKm(sum businessKm)` for the filtered set (keep it OUTSIDE the virtualized window so it always shows).
    Do NOT compute deemed/actual cost here and do NOT import server actions — this component is purely presentational over props (Pitfall 3). Remove all tripType/mixedSplit/privateKm concepts.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>TripTable renders virtualized rows over LogbookTripRecord[], owns its month filter, and exposes edit/delete via callbacks; type-checks clean; no cost math, no tripType.</done>
</task>

<task type="auto">
  <name>Task 2: Prove virtualization (PERF-02) and filter throughput (PERF-03)</name>
  <files>src/components/individual-tax/tax-tools/trip-table.test.tsx</files>
  <action>
    Using the 06-01 recipe (`mockScrollElementSize` + global ResizeObserver polyfill), write:
    1. PERF-02 bounded-DOM proof: build 10,000 `LogbookTripRecord` fixtures, render `<TripTable trips={...} onEditTrip onDeleteTrip />`, assert `container.querySelectorAll("[data-virtual-row]").length` is > 0 and < 200 (bounded window, NOT 10,000). This is the definitive virtualization assertion (research Pitfall 4).
    2. Filter behavior: with a small fixed set spanning two months, select a month and assert only that month's rows are represented (query rendered dates), and "All Months" restores them.
    3. PERF-03 throughput proxy (mirrors import-pipeline.integration.test.ts:164-195): over a 10,000-trip in-memory array, `Date.now()`-measure the pure filter+sort derivation (extract it as an exported helper `filterAndSortTrips(trips, month)` from trip-table.tsx and call it directly, or measure a re-render) and assert elapsed `toBeLessThan(500)` with a comment that this is a logic-side throughput proxy, not a UI-jank benchmark.
    4. Callback wiring: clicking Edit/Delete on a rendered row invokes onEditTrip/onDeleteTrip with the right trip/id.
    Give component tests an explicit timeout (e.g. 15000) per the TESTING.md precedent.
  </action>
  <verify>
    <automated>npx vitest run src/components/individual-tax/tax-tools/trip-table.test.tsx</automated>
  </verify>
  <done>Test proves <200 row nodes at 10k trips, correct month filtering, a <500ms filter budget over 10k, and correct edit/delete callback dispatch.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/individual-tax/tax-tools/trip-table.test.tsx` passes.
- Bounded-DOM assertion confirms virtualization is active (PERF-02).
- No tripType/mixedSplit/privateKm anywhere in trip-table.tsx.
</verification>

<success_criteria>
The persisted trip table renders a bounded DOM window at 10,000+ trips, filters by month responsively, and surfaces edit/delete through parent callbacks — the PERF-02/PERF-03 UI substrate for the container.
</success_criteria>

<output>
After completion, create `.planning/phases/06-logbook-ui-import-wizard-performance/06-02-SUMMARY.md` recording the final `TripTableProps`, the row height used, and the measured bounded node count at 10k.
</output>

---
phase: 07-calculator-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/individual-tax/tax-tools/rental-tab.tsx
  - src/components/individual-tax/tax-tools/home-office-tab.tsx
  - src/components/individual-tax/tax-tools/rental-tab.test.tsx
  - src/components/individual-tax/tax-tools/home-office-tab.test.tsx
autonomous: true
requirements: [CALC-05]
must_haves:
  truths:
    - "Rental net income = total income - sum of the 13 SARS-allowable expense categories (rates, levies, insurance, bond interest, repairs, agent fees, advertising, security, garden, utilities, wear & tear, legal, travel-to-property); no capital/improvement field leaks into the deduction"
    - "Home office deduction apportions shared premises costs by floor area (office m2 / total m2, capped at 1) and adds direct costs, x12 for the annual figure"
    - "The salaried-employee warning copy accurately states current SARS practice: salaried non-commission earners CAN qualify if they meet s23(b) (regular + exclusive use, specifically equipped) and perform duties mainly (>50%) at home, but s23(m) restricts them to premises-type costs"
    - "Both calculators are correctly rulepack-free (no per-year rate applies to a rental worksheet or floor-area apportionment)"
  artifacts:
    - path: "src/components/individual-tax/tax-tools/rental-tab.test.tsx"
      provides: "Regression test: net = income - sum(expenses) over all 13 categories"
      contains: "Net Rental Income"
    - path: "src/components/individual-tax/tax-tools/home-office-tab.test.tsx"
      provides: "Regression test: floor-area apportionment + salaried qualification policy"
      contains: "annual"
    - path: "src/components/individual-tax/tax-tools/home-office-tab.tsx"
      provides: "Accurate salaried s23(b)/s23(m) warning copy"
      contains: "s23"
  key_links:
    - from: "src/components/individual-tax/tax-tools/home-office-tab.tsx"
      to: "office/total floor-area ratio"
      via: "Math.min(office/total, 1) applied to shared costs"
      pattern: "Math\\.min\\("
---

<objective>
Confirm and lock the Rental and Home Office calculators against SARS deductible-expense rules (CALC-05). Research verdict: both are essentially correct and correctly rulepack-free — Rental's 13 expense categories match SARS-allowable rental deductions with no capital/improvement leak, and Home Office apportions by floor area correctly. The remaining gaps are (1) no regression tests pin the arithmetic, and (2) the salaried-employee warning copy overstates SARS's position ("very rarely allows") when SARS in fact allows salaried employees who meet s23(b) + >50%-at-home, subject to the s23(m) cost restriction. This plan adds the regression tests and corrects the warning copy, while deliberately keeping the conservative default (salaried -> qualifies=false) to avoid changing the Dashboard summary flow.

Purpose: Prove CALC-05 with tests and make the s23(b)/s23(m) guidance accurate.
Output: rental-tab.test.tsx, home-office-tab.test.tsx, and corrected Home Office warning copy.

Compliance note (research sign-off item #3): the salaried-eligibility POLICY (block-with-warning vs allow-with-warning + apply s23(m)) is a documented product decision. This plan keeps the conservative block-with-warning behaviour (lowest risk, matches the existing summary flow) and only makes the warning text accurate. The SUMMARY MUST flag this as an open policy decision for the practitioner, citing 07-RESEARCH.md "5b. Home Office" + Concrete Change List E. Do NOT change the `qualifies` logic in this plan.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-calculator-audit/07-RESEARCH.md
@src/components/individual-tax/tax-tools/rental-tab.tsx
@src/components/individual-tax/tax-tools/home-office-tab.tsx

<interfaces>
<!-- Both calcs are inline in the components and reachable via rendered inputs. Assert via rendered DOM. -->
<!-- en-ZA gotcha: getByText on a formatted rand value MUST pass { normalizer: (t) => t } — fmt() uses a
     non-breaking-space thousands separator; the default normalizer silently fails exact-string matches. -->
<!-- Existing render-isolation.test.tsx already contains a worked Home Office example:
     ratio = 20/100 = 0.2; shared = 5000+500+300+200 = 6000; direct(repairs+internet) = 100+50 = 150;
     monthly = 6000*0.2 + 150 = 1350; annual = 1350*12 = 16200 (empType default "commission" -> qualifies). -->
<!-- Rental default months = 12; expense keys: rates, levies, insurance, bondInterest, repairs, agentFees,
     advertising, security, garden, utilities, wearTear, legal, travelToProperty. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rental — confirm expense set and add net-income regression test</name>
  <files>src/components/individual-tax/tax-tools/rental-tab.test.tsx</files>
  <action>
No code change is expected in rental-tab.tsx (research confirmed the 13 expense categories match SARS-allowable rental deductions and there is no capital/improvement field to leak). Create a new Vitest suite that renders `<RentalTab />` wrapped in `RulePackProvider` + `TaxToolsSummaryProvider` (follow the render-isolation.test.tsx wrapper pattern), types values, and asserts the net.

Worked example: monthlyRent=10000, months default 12 -> gross=120000; otherIncome=0; set two expenses, e.g. rates=1000 and bondInterest=2000 -> expenses=3000; net = 120000 - 3000 = 117000. Assert the "Net Rental Income" figure renders as `fmt(117000)` using `{ normalizer: (t) => t }`.

Add a second assertion that "Bond Interest" is a labelled expense field (only the interest portion of the bond is deductible, not capital repayment — the field label already encodes this) and that there is NO field for capital improvements (guard against a future capital-cost leak): assert `screen.queryByLabelText(/improvement/i)` is null.

Use `@testing-library/user-event` `userEvent.setup()` and `getByLabelText` as in render-isolation.test.tsx.
  </action>
  <verify>
    <automated>npx vitest run src/components/individual-tax/tax-tools/rental-tab.test.tsx</automated>
  </verify>
  <done>Rental net = income - sum(expenses) proven; no capital/improvement input exists; suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Home Office — accurate s23(b)/s23(m) warning copy + apportionment regression test</name>
  <files>src/components/individual-tax/tax-tools/home-office-tab.tsx, src/components/individual-tax/tax-tools/home-office-tab.test.tsx</files>
  <action>
(a) Correct ONLY the salaried warning copy in home-office-tab.tsx (the `ho.empType === "salaried"` red panel). Replace the "SARS very rarely allows..." sentence with an accurate statement, e.g.: "Salaried (non-commission) employees CAN claim a home-office deduction if a part of the home is used regularly and exclusively for work and is specifically equipped for it (s23(b)), AND duties are performed mainly (more than 50%) at home. Note: s23(m) restricts salaried employees to premises-type costs (rent, repairs, and s11(a) home-office expenses) and disallows most other deductions and wear-and-tear on the building." Keep the panel heading and the red styling. Do NOT change `calcHO` or the `qualifies = ho.empType !== "salaried"` logic — the conservative block-with-warning default stays (flagged for future policy decision).

(b) Create home-office-tab.test.tsx: render `<HomeOfficeTab />` wrapped in `RulePackProvider` + `TaxToolsSummaryProvider`. Reproduce the apportionment worked example (totalArea=100, officeArea=20, rentOrInterest=5000, rates=500, electricity=300, cleaning=200, repairs=100, internet=50 -> monthly=1350, annual=16200) and assert both `fmt(1350)` and `fmt(16200)` render with `{ normalizer: (t) => t }` (default empType "commission" -> qualifies). Add a ratio-cap assertion: officeArea > totalArea clamps ratio to 1 (e.g. office=200, total=100 -> ratio 100% -> "Office Ratio" shows 100%). Add a salaried-policy assertion: selecting empType "Salaried Employee" shows the "Qualification" card as "Unlikely" and the annual deduction Highlight as `fmt(0)` (documents the current conservative policy) and the accurate s23 warning text is present (assert the panel contains "s23").
  </action>
  <verify>
    <automated>npx vitest run src/components/individual-tax/tax-tools/home-office-tab.test.tsx src/components/individual-tax/tax-tools/render-isolation.test.tsx</automated>
  </verify>
  <done>Warning copy accurately reflects s23(b)/s23(m); apportionment + ratio-cap + salaried-policy tests pass; render-isolation Home Office cases still pass (calcHO unchanged).</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/individual-tax/tax-tools/rental-tab.test.tsx src/components/individual-tax/tax-tools/home-office-tab.test.tsx src/components/individual-tax/tax-tools/render-isolation.test.tsx` passes.
- `npm test` green (calcHO and rental math unchanged, so no other suite is affected).
</verification>

<success_criteria>
- Rental net-income arithmetic and full SARS-allowable expense set proven by test; no capital-cost input.
- Home Office floor-area apportionment + ratio cap proven by test.
- Salaried warning copy is SARS-accurate (s23(b) gate + s23(m) restriction) while the conservative qualification default is preserved and flagged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-calculator-audit/07-03-SUMMARY.md`. Flag the salaried-eligibility policy as an open product decision (block-with-warning vs allow-with-warning + s23(m)) citing 07-RESEARCH.md "5b. Home Office" + Change List E; confirm Rental required no code change.
</output>

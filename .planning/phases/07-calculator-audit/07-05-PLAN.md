---
phase: 07-calculator-audit
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/individual-tax/tax-tools/retirement-tab.tsx
  - src/components/individual-tax/tax-tools/cgt-tab.tsx
  - src/components/individual-tax/tax-tools/retirement-tab.test.tsx
  - src/components/individual-tax/tax-tools/cgt-tab.test.tsx
autonomous: true
requirements: [CALC-02, CALC-03]
must_haves:
  truths:
    - "Retirement deduction limit applies the correct s11F cap per year: R350,000 for 2025/2026, R430,000 for 2027 (already rulepack-sourced) AND the displayed subtitle/sub labels show the per-year cap, not a hardcoded R350,000"
    - "CGT applies the correct per-year annual exclusion (R40k/R40k/R50k), primary-residence exclusion (R2m/R2m/R3m), death exclusion (R300k/R300k/R440k) and 40% inclusion (already rulepack-sourced) AND the select labels + 'Taxable Portion' card show the per-year figures, not hardcoded R2m/R40k/R300k/40%"
    - "Switching the tax year to 2027 updates every displayed retirement/CGT figure AND its label, proving no hardcoded per-year string remains"
    - "Retirement marginal-rate tax saving at 2027 reads the corrected 2027 brackets (depends on 07-01)"
  artifacts:
    - path: "src/components/individual-tax/tax-tools/retirement-tab.tsx"
      provides: "Per-year cap interpolated into subtitle + ResultCard sub (no hardcoded R350,000)"
      contains: "rulePack.retirement.annualCap"
    - path: "src/components/individual-tax/tax-tools/cgt-tab.tsx"
      provides: "Per-year exclusion/inclusion figures interpolated into select labels + Taxable Portion card"
      contains: "rulePack.cgt"
    - path: "src/components/individual-tax/tax-tools/retirement-tab.test.tsx"
      provides: "Per-year cap regression test (2026 vs 2027)"
      contains: "430000"
    - path: "src/components/individual-tax/tax-tools/cgt-tab.test.tsx"
      provides: "Per-year exclusion/inclusion regression test (2027 = 50k/3m/440k/40%)"
      contains: "50000"
  key_links:
    - from: "src/components/individual-tax/tax-tools/retirement-tab.tsx"
      to: "rulePack.retirement.annualCap"
      via: "interpolated into subtitle and ResultCard sub label"
      pattern: "rulePack\\.retirement\\.annualCap"
    - from: "src/components/individual-tax/tax-tools/cgt-tab.tsx"
      to: "rulePack.cgt"
      via: "interpolated into the three exclusion select labels + inclusion-rate card label"
      pattern: "rulePack\\.cgt\\.(annualExclusion|primaryResidenceExclusion|deathExclusion|inclusionRate)"
---

<objective>
Close the display-only compliance gaps in the Retirement (CALC-02) and CGT (CALC-03) calculators. Research verdict: the CORE MATH for both is already correct and rulepack-sourced per year — Retirement applies the right s11F cap (R350k/R350k/R430k) and CGT applies the right exclusions/inclusion. The only defects are HARDCODED display strings that never change when the year changes: Retirement's subtitle "27.5% cap / R350,000 annual limit" and ResultCard sub "27.5% or R350k"; CGT's select labels "Yes — R2m exclusion" / "No — R40k exclusion" / "Yes — R300k exclusion" and the "Taxable Portion (40%)" card. These show wrong figures for 2027. This plan interpolates the rulepack values into those labels and adds per-year regression tests.

Purpose: Make every displayed retirement/CGT figure per-year-correct and prove it with tests.
Output: Label fixes in retirement-tab.tsx + cgt-tab.tsx and dedicated per-year test files.

Depends on 07-01: the 2027 retirement marginal-rate saving reads the corrected 2027 brackets. (The caps/exclusions themselves were already correct pre-07-01.) Confidence HIGH — no sign-off needed.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-calculator-audit/07-RESEARCH.md
@src/components/individual-tax/tax-tools/retirement-tab.tsx
@src/components/individual-tax/tax-tools/cgt-tab.tsx

<interfaces>
<!-- Hardcoded strings to replace (values already correct in the calc; only the LABELS are stale): -->
<!-- retirement-tab.tsx: subtitle "27.5% cap / R350,000 annual limit"; ResultCard sub="27.5% or R350k". -->
<!-- cgt-tab.tsx: select "Yes — R2m exclusion" (primaryResidenceExclusion), "No — R40k exclusion"
     (annualExclusion), "Yes — R300k exclusion" (deathExclusion); ResultCard label "Taxable Portion (40%)"
     (inclusionRate). -->
<!-- fmt() is imported in both files (en-ZA rand). For labels, either fmt(rulePack.cgt.annualExclusion) or a
     short form is fine; the TESTS assert the ResultCard NUMERIC values (already rulepack-driven) plus that the
     label reflects the per-year figure. -->
<!-- Existing render-isolation.test.tsx already proves CGT exclusion 40000->50000 on 2026->2027 year switch and
     Retirement headroom publish (137500). Those assert NUMERIC card values, unaffected by label changes — do NOT
     edit render-isolation.test.tsx in this plan. -->
<!-- en-ZA gotcha: getByText on formatted rand values needs { normalizer: (t) => t }. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retirement — interpolate the per-year cap into labels + per-year test</name>
  <files>src/components/individual-tax/tax-tools/retirement-tab.tsx, src/components/individual-tax/tax-tools/retirement-tab.test.tsx</files>
  <action>
(a) retirement-tab.tsx — replace the two hardcoded cap strings with rulepack-interpolated values (do NOT touch `calcRetire` — the math is correct):
- Subtitle (currently `27.5% cap / R350,000 annual limit`): interpolate both the percentage and the cap, e.g. `{`${(rulePack.retirement.deductiblePercentageLimit * 100).toFixed(1).replace(/\.0$/, "")}% cap / ${fmt(rulePack.retirement.annualCap)} annual limit`}` (or a clean equivalent that renders "27.5% cap / R430 000 annual limit" for 2027).
- ResultCard `sub="27.5% or R350k"`: interpolate the cap, e.g. `sub={`27.5% or ${fmt(rulePack.retirement.annualCap)}`}`.
Keep `fmt` imported (already is).

(b) retirement-tab.test.tsx — new suite. Render `<TaxTools />`, navigate to Retirement (button name /^retirement$/i, index [0], per render-isolation.test.tsx). With default year 2026: type Annual Remuneration high enough that 27.5% exceeds the cap (e.g. 2,000,000 -> 27.5% = 550,000 > cap) and assert the "Deduction Limit" card shows `fmt(350000)` with `{ normalizer: (t) => t }`. Switch tax year select to 2027 and assert the same card now shows `fmt(430000)` — proving the cap is per-year rulepack-sourced. Also assert the subtitle/sub label text reflects the cap per year (e.g. the 2027 render contains a "430" figure and the 2026 render a "350" figure). Optionally assert the marginal-rate line (reads corrected 2027 brackets via 07-01) but the cap assertions are the required guard.
  </action>
  <verify>
    <automated>npx vitest run src/components/individual-tax/tax-tools/retirement-tab.test.tsx src/components/individual-tax/tax-tools/render-isolation.test.tsx</automated>
  </verify>
  <done>Retirement subtitle/sub labels interpolate the per-year cap; test proves Deduction Limit 350000 (2026) -> 430000 (2027); render-isolation retirement cases still pass.</done>
</task>

<task type="auto">
  <name>Task 2: CGT — interpolate per-year exclusions/inclusion into labels + per-year test</name>
  <files>src/components/individual-tax/tax-tools/cgt-tab.tsx, src/components/individual-tax/tax-tools/cgt-tab.test.tsx</files>
  <action>
(a) cgt-tab.tsx — replace the four hardcoded strings with rulepack-interpolated values (do NOT touch `calcCGT` — the math is correct):
- Primary-residence select option "Yes — R2m exclusion" -> interpolate `rulePack.cgt.primaryResidenceExclusion` (e.g. `Yes — ${fmt(rulePack.cgt.primaryResidenceExclusion)} exclusion`).
- Death select option "No — R40k exclusion" -> interpolate `rulePack.cgt.annualExclusion`.
- Death select option "Yes — R300k exclusion" -> interpolate `rulePack.cgt.deathExclusion`.
- ResultCard label "Taxable Portion (40%)" -> interpolate the inclusion rate, e.g. `Taxable Portion (${(rulePack.cgt.inclusionRate * 100).toFixed(0)}%)`.

(b) cgt-tab.test.tsx — new suite. Render `<TaxTools />`, navigate to CGT (button name /capital gains/i, index [0]). At default 2026, assert a CGT worked example still computes correctly reading rulepack rates (reuse the render-isolation example: taxableIncome 2,000,000; proceeds 1,000,000; base 400,000; improvements 50,000; selling 20,000 -> cgtPayable `fmt(88200)`). Switch tax year to 2027 and assert: (i) the primary-residence option label reflects `fmt(3000000)`, the death option labels reflect `fmt(50000)` and `fmt(440000)`, and the "Taxable Portion" card label contains "40%"; (ii) with a primary-residence disposal, the 2027 primary-residence exclusion (R3,000,000) is applied — construct an example where the 2026 vs 2027 primary-residence exclusion changes the netGain/cgtPayable, and assert the 2027 figure. Use `{ normalizer: (t) => t }` on all `fmt()` matches. Run to confirm the exact cgtPayable literals.
  </action>
  <verify>
    <automated>npx vitest run src/components/individual-tax/tax-tools/cgt-tab.test.tsx src/components/individual-tax/tax-tools/render-isolation.test.tsx</automated>
  </verify>
  <done>CGT select labels + Taxable Portion card interpolate per-year figures; test proves 2027 shows 50k/3m/440k/40% and applies the R3m primary-residence exclusion; render-isolation CGT cases still pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/individual-tax/tax-tools/retirement-tab.test.tsx src/components/individual-tax/tax-tools/cgt-tab.test.tsx src/components/individual-tax/tax-tools/render-isolation.test.tsx` passes.
- `npm test` green (calc math unchanged; only labels changed).
- Manual grep guard: no literal "R350,000"/"R350k"/"R2m"/"R40k"/"R300k"/"(40%)" remains as a hardcoded per-year string in retirement-tab.tsx or cgt-tab.tsx.
</verification>

<success_criteria>
- Every displayed retirement/CGT per-year figure and its label is rulepack-sourced; switching to 2027 updates both value and label.
- Per-year regression tests prove Retirement cap 350000->430000 and CGT 40k/2m/300k/40% -> 50k/3m/440k/40%.
- No hardcoded per-year display string remains.
</success_criteria>

<output>
After completion, create `.planning/phases/07-calculator-audit/07-05-SUMMARY.md`. Note that the core Retirement/CGT math was already correct and rulepack-sourced (Phase 1); this plan closed the display-only gaps and added per-year regression proofs. Cite 07-RESEARCH.md "2. Retirement" / "3. CGT" + Concrete Change List D.
</output>

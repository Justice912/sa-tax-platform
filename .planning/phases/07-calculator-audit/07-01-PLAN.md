---
phase: 07-calculator-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/individual-tax/rules-2027.ts
  - src/modules/individual-tax/rulepack.test.ts
  - src/components/individual-tax/tax-tools/calc-helpers.test.ts
autonomous: true
requirements: [CALC-06]
must_haves:
  truths:
    - "Selecting tax year 2027 produces tax/rebate/threshold figures that match the gazetted Budget-2026 2026/27 tables (bracket-1 ceiling R245,100; primary rebate R17,820; under-65 threshold R99,000), NOT the pre-Budget estimates currently in the file"
    - "calcTax and getMarginalRate return different, per-year-correct results for 2025 vs 2027 (proving the shared calc reads brackets live from the rulepack for the selected year — CALC-06)"
    - "The rulepack completeness/assertion tests assert the CORRECTED 2027 values, so a future regression back to the wrong estimates fails the build"
    - "2027 medical (376/254), retirement cap (430,000) and CGT (50k/3m/440k, 40%) remain unchanged — they were already correct"
  artifacts:
    - path: "src/modules/individual-tax/rules-2027.ts"
      provides: "Corrected gazetted 2027 tax brackets, rebates and thresholds"
      contains: "max: 245100"
    - path: "src/modules/individual-tax/rulepack.test.ts"
      provides: "Updated 2027 assertions matching the corrected data"
      contains: "primary: 17820"
    - path: "src/components/individual-tax/tax-tools/calc-helpers.test.ts"
      provides: "Per-year calcTax/getMarginalRate proof (2025 vs 2027) — CALC-06 guard"
      contains: "getMarginalRate"
  key_links:
    - from: "src/components/individual-tax/tax-tools/calc-helpers.ts"
      to: "src/modules/individual-tax/rules-2027.ts"
      via: "calcTax(rulePack, taxable) reads rulePack.taxBrackets"
      pattern: "rulePack\\.taxBrackets"
    - from: "src/modules/individual-tax/rulepack.test.ts"
      to: "src/modules/individual-tax/rules-2027.ts"
      via: "getIndividualTaxRulePack(2027) assertions"
      pattern: "getIndividualTaxRulePack\\(2027\\)"
---

<objective>
Correct the 2027 rulepack DATA (tax brackets, rebates, age thresholds) to the gazetted Budget-2026 2026/27 figures. This is the single highest-impact fix in Phase 7: every calculator reads these values live via `useRulePack()`, so the current pre-Budget estimates silently corrupt ALL 2027 output (provisional tax, marginal rates, any threshold logic). `rulepack.test.ts` currently asserts the WRONG values, locking the bug in — it must be updated in lockstep. This plan closes CALC-06 (Dashboard/shared-calc bracket & rebate figures sourced correctly per year) and unblocks the correct 2027 regression tests every downstream calculator plan (07-04, 07-05) depends on.

Purpose: Fix the data root-cause before auditing calculator logic, so downstream 2027 tests assert against correct figures.
Output: Corrected `rules-2027.ts`, updated `rulepack.test.ts`, and a new per-year `calc-helpers.test.ts` guard proving bracket/marginal figures are rulepack-sourced per selected year.

Compliance note: The 2027 bracket/rebate/threshold values below are HIGH confidence (three independent sources: SARS Rates of Tax, SARS Budget 2026 FAQ, ftomasek third-party). Per research "Needs Human Compliance Sign-Off #4", a practitioner should confirm against the final SARS 2026/27 tax tables before release — but do NOT block execution; encode the values as test expectations so any drift fails loudly. Cite `.planning/phases/07-calculator-audit/07-RESEARCH.md` (reference table + Concrete Change List A) in the SUMMARY.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-calculator-audit/07-RESEARCH.md

<interfaces>
<!-- The shared calc reads brackets from the rulepack. Note the calcTax "+1" convention:
     baseTax at bracket N equals cumulative tax at the ceiling of bracket N-1. -->

From src/components/individual-tax/tax-tools/calc-helpers.ts:
```typescript
export function calcTax(rulePack: IndividualTaxRulePack, taxable: number): number;
//   finds bracket where taxable in [min, max]; returns b.baseTax + (taxable - b.min + 1) * b.rate
export function getMarginalRate(rulePack: IndividualTaxRulePack, taxable: number): number;
//   returns the rate of the bracket containing `taxable` (0.18 if <=0, 0.45 if none)
```

From src/modules/individual-tax/rulepack-registry.ts:
```typescript
export function getIndividualTaxRulePack(year: SupportedAssessmentYear): IndividualTaxRulePack;
```

CURRENT (WRONG) rules-2027.ts values to replace — pre-Budget estimates:
  taxBrackets[0].max = 247100 (should be 245100), baseTax chain 44478/80384/126853/201661/527589/625794,
  a fabricated 6th boundary at 1578100, rebates {18395,10077,3356}, thresholds {104758,162689,182850}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the 2027 tax brackets, rebates and thresholds in rules-2027.ts</name>
  <files>src/modules/individual-tax/rules-2027.ts</files>
  <action>
Replace the `taxBrackets`, `rebates`, and `thresholds` blocks with the gazetted Budget-2026 2026/27 values from 07-RESEARCH.md (Authoritative SARS Rules table + Concrete Change List A). Leave EVERYTHING ELSE untouched — `medicalTaxCredit` (376/254), `retirement.annualCap` (430000), `cgt` (50000/440000/0.40/3000000), `interestExemption`, `travelDeemedCostTable`, `provisionalTax`, `periodStart/End`, and `sourceReference` are already correct (Phase 1 verified) and MUST NOT change.

Set `taxBrackets` to exactly (note the `+1` min convention already used across the repo — each `min` is prior `max` + 1; `baseTax` at bracket N = cumulative tax at the ceiling of bracket N-1; rates are unchanged from prior years):
```typescript
  taxBrackets: [
    { min: 1, max: 245100, baseTax: 0, rate: 0.18 },
    { min: 245101, max: 383100, baseTax: 44118, rate: 0.26 },
    { min: 383101, max: 530200, baseTax: 79998, rate: 0.31 },
    { min: 530201, max: 695800, baseTax: 125599, rate: 0.36 },
    { min: 695801, max: 887000, baseTax: 185215, rate: 0.39 },
    { min: 887001, max: 1878600, baseTax: 259783, rate: 0.41 },
    { min: 1878601, max: null, baseTax: 666339, rate: 0.45 },
  ],
```
Set `rebates` to `{ primary: 17820, secondary: 9765, tertiary: 3249 }`.
Set `thresholds` to `{ under65: 99000, age65To74: 153250, age75Plus: 171300 }`.

Why these exact values: the fabricated 6th bracket boundary (1578100) and the estimate ceilings/rebates were pre-Budget-2026 guesses. The gazetted table adjusts brackets/rebates/thresholds ~3.4% for inflation (first inflationary adjustment since 2023/24). The baseTax chain above is internally consistent with `calcTax`'s `(taxable - min + 1) * rate` accumulation.
  </action>
  <verify>
    <automated>npx vitest run src/modules/individual-tax/rulepack-completeness.test.ts</automated>
  </verify>
  <done>rules-2027.ts bracket-1 max is 245100, primary rebate 17820, under65 threshold 99000; completeness test (which checks structure + the already-correct 2027 retirement/CGT/travel values) still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update rulepack.test.ts 2027 assertions to the corrected values</name>
  <files>src/modules/individual-tax/rulepack.test.ts</files>
  <action>
In the `"uses the published 2027 bracket and rebate updates"` test, update the assertions that currently lock in the WRONG estimates:
- `taxBrackets[0]` matchObject: change `max: 247100` -> `max: 245100` (keep `min: 1, rate: 0.18`).
- `rebates` toEqual: change `{ primary: 18395, secondary: 10077, tertiary: 3356 }` -> `{ primary: 17820, secondary: 9765, tertiary: 3249 }`.
- `thresholds` toEqual: change `{ under65: 104758, age65To74: 162689, age75Plus: 182850 }` -> `{ under65: 99000, age65To74: 153250, age75Plus: 171300 }`.
- Leave the `medicalTaxCredit` (376/254), `retirement.annualCap` (430000) and `cgt` assertions unchanged — they are correct.
Do not weaken any assertion; only swap the numeric expectations to the corrected figures.
  </action>
  <verify>
    <automated>npx vitest run src/modules/individual-tax/rulepack.test.ts</automated>
  </verify>
  <done>rulepack.test.ts passes against the corrected rules-2027.ts, asserting max 245100, rebates {17820,9765,3249}, thresholds {99000,153250,171300}.</done>
</task>

<task type="auto">
  <name>Task 3: Add per-year calc-helpers guard test (CALC-06)</name>
  <files>src/components/individual-tax/tax-tools/calc-helpers.test.ts</files>
  <action>
Create a new Vitest suite proving the shared calc functions read brackets live from the rulepack for the selected year (the CALC-06 guard that a regression to wrong 2027 data would fail). Import `calcTax`, `getMarginalRate` from `@/components/individual-tax/tax-tools/calc-helpers` and `getIndividualTaxRulePack` from `@/modules/individual-tax/rulepack-registry`.

Assert exact per-year values (arithmetic derived from the `baseTax + (taxable - min + 1) * rate` formula):
- `calcTax(getIndividualTaxRulePack(2025), 500000)` === 117507  (bracket 3: 77362 + (500000-370501+1)*0.31).
- `calcTax(getIndividualTaxRulePack(2027), 500000)` === 116237  (bracket 3: 79998 + (500000-383101+1)*0.31).
- Assert these two differ — proving the figure is year-sourced, not a constant.
- `getMarginalRate(getIndividualTaxRulePack(2025), 880000)` === 0.41  (2025 bracket 6 starts at 857901).
- `getMarginalRate(getIndividualTaxRulePack(2027), 880000)` === 0.39  (2027 bracket 5 is 695801-887000; the R887,000 boundary moved up).
- Sanity: `getIndividualTaxRulePack(2027).rebates.primary` === 17820 (rebate sourced per year).

These are plain numeric comparisons (no DOM / no en-ZA formatting), so no `normalizer` is needed here. Run the exact values first; if the repo's rounding differs from the hand arithmetic, trust the run and correct the expected literal, but the 2025-vs-2027 INEQUALITY assertions must hold.
  </action>
  <verify>
    <automated>npx vitest run src/components/individual-tax/tax-tools/calc-helpers.test.ts</automated>
  </verify>
  <done>New suite passes; calcTax(2025,500000) != calcTax(2027,500000) and getMarginalRate(2025,880000) != getMarginalRate(2027,880000), proving per-year rulepack sourcing.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/modules/individual-tax/rulepack.test.ts src/modules/individual-tax/rulepack-completeness.test.ts src/components/individual-tax/tax-tools/calc-helpers.test.ts` all pass.
- Full suite `npm test` is green — confirm the only 2027-asserting tests elsewhere (calculation-service.test.ts uses `toBeGreaterThan` loose bounds; schedules.test.ts asserts interest-exemption-only taxableIncome 5500) still pass unchanged. If any unexpected 2027-derived figure breaks, reconcile it to the corrected data (do not revert the data fix).
- Manual diff check: medical (376/254), retirement cap (430000), CGT (50000/440000/3000000/0.40), travelDeemedCostTable in rules-2027.ts are byte-unchanged.
</verification>

<success_criteria>
- rules-2027.ts carries the gazetted 2027 brackets/rebates/thresholds; the fabricated 1578100 boundary and pre-Budget rebate/threshold estimates are gone.
- rulepack.test.ts asserts the corrected values (no wrong value remains as an expectation).
- calc-helpers.test.ts proves 2025 vs 2027 divergence for both calcTax and getMarginalRate.
- `npm test` passes end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/07-calculator-audit/07-01-SUMMARY.md`. In it, explicitly flag: (1) the 2027 gazetted values are HIGH confidence but pending practitioner confirmation against final SARS 2026/27 tables (research sign-off item #4); (2) which downstream figures this unblocked (2027 provisional fullTax, 2027 marginal rates for retirement/CGT). Cite 07-RESEARCH.md reference table + Change List A.
</output>

---
phase: 05-component-decomposition
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/components/individual-tax/tax-tools.tsx
  - src/components/individual-tax/tax-tools/cgt-tab.tsx
  - src/components/individual-tax/tax-tools/retirement-tab.tsx
  - src/components/individual-tax/tax-tools/render-isolation.test.tsx
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Capital Gains and Retirement are standalone components with colocated state, each reading rates from RulePackContext"
    - "Changing the tax-year selector updates CGT and Retirement figures (rulepack-sourced) exactly as before"
    - "Typing in Retirement does not re-render CGT (proven by Profiler)"
    - "Retirement headroom still flows to the Dashboard"
  artifacts:
    - path: "src/components/individual-tax/tax-tools/cgt-tab.tsx"
      provides: "CgtTab: cgt state + calcCGT + JSX, reads rulePack.cgt.* + getMarginalRate via useRulePack"
      min_lines: 130
    - path: "src/components/individual-tax/tax-tools/retirement-tab.tsx"
      provides: "RetirementTab: ret state + calcRetire + JSX, reads rulePack.retirement.* + getMarginalRate; publishes retirementHeadroom"
      min_lines: 100
  key_links:
    - from: "src/components/individual-tax/tax-tools/cgt-tab.tsx"
      to: "src/components/individual-tax/tax-tools/rulepack-context.tsx"
      via: "useRulePack() for rulePack.cgt.* and getMarginalRate"
      pattern: "useRulePack\\(|rulePack.cgt"
    - from: "src/components/individual-tax/tax-tools/retirement-tab.tsx"
      to: "src/components/individual-tax/tax-tools/rulepack-context.tsx"
      via: "useRulePack() for rulePack.retirement.* and getMarginalRate"
      pattern: "useRulePack\\(|rulePack.retirement"
    - from: "src/components/individual-tax/tax-tools/retirement-tab.tsx"
      to: "src/components/individual-tax/tax-tools/summary-context.tsx"
      via: "useSummaryWriter publish of retirementHeadroom"
      pattern: "retirementHeadroom"
---

<objective>
Extract the Capital Gains and Retirement calculators — the first two that depend on the rulepack — into standalone components, each consuming rates through `useRulePack()` (proving success criterion 3: calculators read shared values from rulepack-derived context, not local constants). Retirement publishes its headroom to the summary context; CGT is not on the Dashboard, so it publishes nothing. Extend the render-isolation test to cover this pair.

Purpose: Proves the `useRulePack()` consumption pattern and year-switch reactivity survive extraction, on two mid-complexity calculators.
Output: `cgt-tab.tsx`, `retirement-tab.tsx`, extended `render-isolation.test.tsx`, thinner shell.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-decomposition/05-RESEARCH.md
@src/components/individual-tax/tax-tools.tsx
@.planning/phases/05-component-decomposition/05-03-SUMMARY.md

<constraints>
- REFACTOR — byte-identical outputs. Copy CGT and Retirement math VERBATIM.
  - CGT: `gain = proceeds - base - impr - sell`; `exclusion = death ? rulePack.cgt.deathExclusion : rulePack.cgt.annualExclusion`, plus `min(gain, rulePack.cgt.primaryResidenceExclusion)` when `primaryRes && gain > 0`; `netGain = max(0, gain - exclusion)`; `taxableGain = netGain * rulePack.cgt.inclusionRate`; `marginal = getMarginalRate(rulePack, taxInc)`; `cgtPayable`, `effectiveRate` exactly as current.
  - Retirement: `limit = min(inc * rulePack.retirement.deductiblePercentageLimit, rulePack.retirement.annualCap)`; `headroom = max(0, limit - current)`; `marginal = getMarginalRate(rulePack, inc)`; `saving = usable * marginal` exactly as current.
- Both read rates via `const { rulePack } = useRulePack();` and import `getMarginalRate` from `calc-helpers`. No hardcoded rate constants.
- Colocated `useState`; remove `cgt`/`ret` state + `calcCGT`/`calcRetire` + `cgtResult`/`retResult` from the shell. Remove the shell's `retirementHeadroom` publish (RetirementTab now owns it). CGT has no Dashboard publish.
- Always-mounted + CSS-hidden wrappers, not conditional mount.
- Calculators use `useSummaryWriter()` only (Retirement), never `useSummary()`.
- Build gate `npm run build` (Turbopack); no `--webpack`.
</constraints>

<interfaces>
Source anchors (locate by `{tab === "cgt"...}` / `{tab === "retirement"...}` markers):
- CGT: state `cgt` (~218-227), `calcCGT` (~431-458), JSX `{tab === "cgt" && (...)}` (~1635-1775).
- Retirement: state `ret` (~209-215), `calcRetire` (~411-428), JSX `{tab === "retirement" && (...)}` (~1534-1634).

```tsx
import { useRulePack } from "@/components/individual-tax/tax-tools/rulepack-context";
import { getMarginalRate } from "@/components/individual-tax/tax-tools/calc-helpers";
// RetirementTab additionally:
import { useSummaryWriter } from "@/components/individual-tax/tax-tools/summary-context";
// const { rulePack } = useRulePack();  →  rulePack.cgt.* / rulePack.retirement.*
```
Summary key already defined: `retirementHeadroom`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CgtTab</name>
  <files>src/components/individual-tax/tax-tools/cgt-tab.tsx</files>
  <action>
Create `cgt-tab.tsx` (`"use client"`). Move the `cgt` `useState` object, the `calcCGT` logic (compute `cgtResult` locally, verbatim, reading `rulePack.cgt.{deathExclusion,annualExclusion,primaryResidenceExclusion,inclusionRate}` and `getMarginalRate(rulePack, taxInc)`), and the `{tab === "cgt" && (...)}` JSX. `const { rulePack } = useRulePack();`. Import `StatCard`/`ResultCard`/`Highlight`/`Field`/`fmt`/`inputCls`/`selectCls` from `shared` and `getMarginalRate` from `calc-helpers`. No summary publish (CGT is not on the Dashboard).
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit</automated>
  </verify>
  <done>CgtTab exists, reads rulePack.cgt.* via useRulePack, verbatim math; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract RetirementTab</name>
  <files>src/components/individual-tax/tax-tools/retirement-tab.tsx</files>
  <action>
Create `retirement-tab.tsx` (`"use client"`). Move the `ret` `useState` object, the `calcRetire` logic (verbatim, reading `rulePack.retirement.deductiblePercentageLimit`/`annualCap` and `getMarginalRate(rulePack, inc)`), and the `{tab === "retirement" && (...)}` JSX. `const { rulePack } = useRulePack();`. Add `useSummaryWriter()` + `useEffect` publishing `retResult.headroom` to `"retirementHeadroom"`. Import from `shared` and `calc-helpers`.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit</automated>
  </verify>
  <done>RetirementTab exists, reads rulePack.retirement.* via useRulePack, publishes headroom; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire both tabs into the shell and extend the render-isolation test</name>
  <files>src/components/individual-tax/tax-tools.tsx, src/components/individual-tax/tax-tools/render-isolation.test.tsx</files>
  <action>
Shell edits (`TaxToolsInner`): delete `cgt`/`ret` state, `calcCGT`/`calcRetire`, `cgtResult`/`retResult`, and the `retirementHeadroom` `useEffect` publish. Replace the two inline JSX blocks with `<div className={tab === "cgt" ? "" : "hidden"}><CgtTab /></div>` and the equivalent `<RetirementTab />`. Add imports.

Extend `render-isolation.test.tsx`: add a Profiler isolation test for `RetirementTab` vs `CgtTab` (both under `<RulePackProvider><TaxToolsSummaryProvider>`, each in its own Profiler) — type in Retirement, assert CGT's profiler `not.toHaveBeenCalled()`. Add an output-preservation assertion for at least one of them (e.g. type a proceeds/base into CGT, assert displayed CGT Payable matches expected `fmt(...)`). Optionally add a year-switch assertion: render the tab under a provider, and confirm a rulepack-driven figure differs between two `SupportedAssessmentYear` values (or assert the 2027 CGT annual exclusion path) — keep it minimal but prove rates come from context.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Shell renders CgtTab/RetirementTab via hidden-mounted wrappers, inline copies removed; extended isolation test passes; full suite green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean; `npm test` passes.
- `npm run build` (Turbopack) succeeds.
- Profiler test proves Retirement typing does not re-render CGT.
- Year switch still updates CGT/Retirement figures; Dashboard retirement headroom still correct.
</verification>

<success_criteria>
- CGT and Retirement are standalone, colocated-state components reading rates via `useRulePack()` (criteria 1 + 3, 4 of 8 calculators done).
- Render isolation extended and passing.
- Behaviour identical (tests + build green).
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-decomposition/05-04-SUMMARY.md`.
</output>

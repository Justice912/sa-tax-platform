---
phase: 05-component-decomposition
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - src/components/individual-tax/tax-tools.tsx
  - src/components/individual-tax/tax-tools/medical-tab.tsx
  - src/components/individual-tax/tax-tools/provisional-tax-tab.tsx
  - src/components/individual-tax/tax-tools/render-isolation.test.tsx
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Medical Credits and Provisional Tax are standalone components with colocated state, reading rates from RulePackContext"
    - "The Phase-1-corrected provisional safe-harbour branch orientation (0.90 at/below R1m, 0.80 above) is preserved verbatim"
    - "Typing in Medical does not re-render Provisional Tax (proven by Profiler)"
    - "Medical total still flows to the Dashboard"
  artifacts:
    - path: "src/components/individual-tax/tax-tools/medical-tab.tsx"
      provides: "MedicalTab: med state + calcMedical (s6A rulepack-sourced, s6B untouched) + JSX; publishes medicalTotal"
      min_lines: 100
    - path: "src/components/individual-tax/tax-tools/provisional-tax-tab.tsx"
      provides: "ProvisionalTaxTab: prov state + calcProv (calcTax + safe-harbour verbatim) + JSX"
      min_lines: 110
  key_links:
    - from: "src/components/individual-tax/tax-tools/medical-tab.tsx"
      to: "src/components/individual-tax/tax-tools/rulepack-context.tsx"
      via: "useRulePack() for rulePack.medicalTaxCredit.*"
      pattern: "useRulePack\\(|medicalTaxCredit"
    - from: "src/components/individual-tax/tax-tools/medical-tab.tsx"
      to: "src/components/individual-tax/tax-tools/summary-context.tsx"
      via: "useSummaryWriter publish of medicalTotal"
      pattern: "medicalTotal"
    - from: "src/components/individual-tax/tax-tools/provisional-tax-tab.tsx"
      to: "src/components/individual-tax/tax-tools/calc-helpers.ts"
      via: "calcTax(rulePack, estTaxable)"
      pattern: "calcTax\\("
---

<objective>
Extract the Medical Credits and Provisional Tax calculators. Both read rates via `useRulePack()`. Two verbatim-preservation constraints matter here: the s6B medical multipliers (0.333/0.25/0.075/3×) and the `payment < safeHarbour * 0.8` risk-band heuristic are explicitly Phase 7 scope — copy them unchanged; and the Phase-1-corrected provisional safe-harbour branch orientation must survive extraction exactly (a mechanical copy that "tidies" the ternary risks re-inverting it — research Pitfall 4). Medical publishes its total to the Dashboard summary; Provisional is not on the Dashboard.

Purpose: Extracts the two calculators carrying prior-phase correctness fixes, under strict verbatim constraints, with a spot-check that the safe-harbour orientation is intact.
Output: `medical-tab.tsx`, `provisional-tax-tab.tsx`, extended `render-isolation.test.tsx`, thinner shell.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-decomposition/05-RESEARCH.md
@src/components/individual-tax/tax-tools.tsx
@.planning/phases/05-component-decomposition/05-04-SUMMARY.md

<constraints>
- REFACTOR — byte-identical outputs. Copy Medical and Provisional math VERBATIM.
- Medical: s6A uses `rulePack.medicalTaxCredit.firstTwoMembersPerMonth`/`additionalMemberPerMonth` (rulepack-sourced, keep). The s6B branch (`age !== "under65" || disability` → `qual * 0.333`; else `qual = oop - 0.075*taxInc - 3*s6a` → `qual * 0.25`) and the `3 * s6a` term are hardcoded formula constants explicitly OUT OF SCOPE (Phase 7 CALC-01) — copy them unchanged, do NOT "fix" or rulepack-source them.
- Provisional (research Pitfall 4 — CRITICAL): copy the `calcProv` block verbatim, including:
  `safeHarbour = estTaxable > pt.safeHarbourTaxableIncomeThreshold ? priorTax * pt.safeHarbourActualPctAboveThreshold : priorTax * pt.safeHarbourBasicAmountOrActualPctBelowThreshold`
  Do NOT reorder or simplify this ternary — the branch orientation is a Phase-1 bug fix. Also keep `fullTax = calcTax(rulePack, estTaxable) - rulePack.rebates.primary`, the P1/P2 payment split, and the `payment < safeHarbour * 0.8` risk band unchanged.
- Both use `const { rulePack } = useRulePack();`. Provisional imports `calcTax` from `calc-helpers`.
- Colocated `useState`; remove `med`/`prov` state, `calcMedical`/`calcProv`, `medResult`/`provResult`, and the shell's `medicalTotal` publish. Provisional has no Dashboard publish.
- Always-mounted + CSS-hidden wrappers.
- Medical uses `useSummaryWriter()` only, never `useSummary()`.
- Build gate `npm run build` (Turbopack); no `--webpack`.
</constraints>

<interfaces>
Source anchors (locate by `{tab === "medical"...}` / `{tab === "provisional"...}` markers):
- Medical: state `med` (~199-206), `calcMedical` (~385-408), JSX `{tab === "medical" && (...)}` (~1430-1533).
- Provisional: state `prov` (~230-237), `calcProv` (~461-490), JSX `{tab === "provisional" && (...)}` (~1776-1893).
Summary key already defined: `medicalTotal`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract MedicalTab</name>
  <files>src/components/individual-tax/tax-tools/medical-tab.tsx</files>
  <action>
Create `medical-tab.tsx` (`"use client"`). Move the `med` `useState` object, `calcMedical` (verbatim — s6A rulepack-sourced, s6B multipliers and `3*s6a` term UNCHANGED per Phase 7 scope note), and the `{tab === "medical" && (...)}` JSX. `const { rulePack } = useRulePack();`. Add `useSummaryWriter()` + `useEffect` publishing `medResult.total` to `"medicalTotal"`. Import from `shared`.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit</automated>
  </verify>
  <done>MedicalTab exists, s6A rulepack-sourced + s6B verbatim, publishes medicalTotal; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ProvisionalTaxTab (preserve safe-harbour orientation verbatim)</name>
  <files>src/components/individual-tax/tax-tools/provisional-tax-tab.tsx</files>
  <action>
Create `provisional-tax-tab.tsx` (`"use client"`). Move the `prov` `useState` object, `calcProv` (VERBATIM — do not reorder the safe-harbour ternary; keep `calcTax(rulePack, estTaxable) - rulePack.rebates.primary`, the P1/P2 split, and the risk band), and the `{tab === "provisional" && (...)}` JSX. `const { rulePack } = useRulePack();`. Import `calcTax` from `calc-helpers` and presentational pieces from `shared`. No summary publish.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit</automated>
  </verify>
  <done>ProvisionalTaxTab exists with verbatim calcProv (safe-harbour orientation intact); tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire both tabs into the shell, extend isolation test, and spot-check safe-harbour</name>
  <files>src/components/individual-tax/tax-tools.tsx, src/components/individual-tax/tax-tools/render-isolation.test.tsx</files>
  <action>
Shell edits: delete `med`/`prov` state, `calcMedical`/`calcProv`, `medResult`/`provResult`, and the `medicalTotal` `useEffect` publish. Replace the two inline JSX blocks with `<div className={tab === "medical" ? "" : "hidden"}><MedicalTab /></div>` and the equivalent `<ProvisionalTaxTab />`. Add imports.

Extend `render-isolation.test.tsx`:
- Add a Profiler isolation test: type in Medical, assert Provisional's profiler `not.toHaveBeenCalled()`.
- Add a provisional safe-harbour spot-check (2026, threshold R1,000,000): with `priorTax` set and `estTaxable` at/below R1m, assert the displayed Safe Harbour reflects the ×0.90 branch; with `estTaxable` above R1m, assert it reflects the ×0.80 branch. This proves the Phase-1 orientation survived extraction (research Pitfall 4). Assert via the rendered Safe Harbour ResultCard value.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Shell renders MedicalTab/ProvisionalTaxTab via hidden-mounted wrappers, inline copies removed; isolation + safe-harbour spot-check pass; full suite green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean; `npm test` passes (incl. safe-harbour spot-check).
- `npm run build` (Turbopack) succeeds.
- Profiler test proves Medical typing does not re-render Provisional.
- Dashboard medical total still correct.
</verification>

<success_criteria>
- Medical and Provisional are standalone, colocated-state, rulepack-context-reading components (6 of 8 calculators done).
- Phase-1 safe-harbour fix and Phase-7-scoped s6B constants preserved verbatim, spot-checked.
- Behaviour identical (tests + build green).
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-decomposition/05-05-SUMMARY.md`.
</output>

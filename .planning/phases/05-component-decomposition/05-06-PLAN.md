---
phase: 05-component-decomposition
plan: 06
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - src/components/individual-tax/tax-tools.tsx
  - src/components/individual-tax/tax-tools/travel-logbook-tab.tsx
  - src/components/individual-tax/tax-tools/render-isolation.test.tsx
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Travel Logbook is a standalone component owning all its state (trips, trip form, upload/import wizard, filters, vehicle inputs) plus its own toast/notify"
    - "Typing in the Travel tab's vehicle value does not re-render any other calculator (proven by Profiler)"
    - "Travel deduction still flows to the Dashboard; deemed-cost rates still come from the rulepack via useRulePack"
    - "After this plan the shell is thin: tab state + nav + year selector + 8 always-mounted CSS-hidden tab wrappers, with no calculator math left inline"
  artifacts:
    - path: "src/components/individual-tax/tax-tools/travel-logbook-tab.tsx"
      provides: "TravelLogbookTab: all travel/logbook state, trip CRUD, CSV upload + import wizard, deemed-cost calc via useRulePack, memoized trip aggregates, local toast, publishes travelDeduction"
      min_lines: 600
  key_links:
    - from: "src/components/individual-tax/tax-tools.tsx"
      to: "src/components/individual-tax/tax-tools/travel-logbook-tab.tsx"
      via: "always-mounted CSS-hidden wrapper"
      pattern: "TravelLogbookTab"
    - from: "src/components/individual-tax/tax-tools/travel-logbook-tab.tsx"
      to: "src/components/individual-tax/tax-tools/rulepack-context.tsx"
      via: "useRulePack() for getDeemedRate(rulePack, ...)"
      pattern: "useRulePack\\(|getDeemedRate"
    - from: "src/components/individual-tax/tax-tools/travel-logbook-tab.tsx"
      to: "src/components/individual-tax/tax-tools/summary-context.tsx"
      via: "useSummaryWriter publish of travelDeduction"
      pattern: "travelDeduction"
---

<objective>
Extract the largest tab — Travel Logbook (~658 lines of JSX plus 10 state slots, trip CRUD, the naive CSV-upload/import wizard, deemed-cost calculation, monthly chart, CSV export, and the file input + toast/notify that today live at shell level even though only Travel uses them) — into a single standalone `TravelLogbookTab`. It reads deemed-cost rates via `useRulePack()`, publishes `travelDeduction` to the summary context, and owns its own toast. This is the final calculator extraction: once done, the shell is a thin container. Also memoize the trip aggregates (`tripStats`, `monthlyData`, `filteredTrips`) with `useMemo` — cheap while the code is open, and it keeps Phase 6's virtualization from starting on a pathological per-keystroke O(n) recompute (research Pitfall 3).

IMPORTANT SCOPE BOUNDARY: extract the EXISTING in-memory `Trip[]` + naive `FileReader`/comma-split upload behaviour AS-IS. Do NOT wire in the Phase 4 import pipeline or the Phase 2/3 logbook domain module — that is Phase 6's job (it depends on this phase landing first). This is a relocation, not a rebuild.

Purpose: Completes the 8-of-8 decomposition and removes the last shared-state source (the trips array + toast) from the shell, which is the concrete mechanism behind the freeze.
Output: `travel-logbook-tab.tsx`, extended `render-isolation.test.tsx`, and a thin `tax-tools.tsx` shell.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-decomposition/05-RESEARCH.md
@src/components/individual-tax/tax-tools.tsx
@.planning/phases/05-component-decomposition/05-05-SUMMARY.md

<constraints>
- REFACTOR — byte-identical behaviour. Copy all trip logic verbatim: `allocTrip`, `newTrip`, `saveTrip`, `changeTripType`, `tripStats`/`bizPct`, deemed-cost (`vVal`/`sRate = getDeemedRate(rulePack, vVal)`/`deemedFixed`/`deemedFuel`/`deemedMaint`/`deemedTotal`/`travelDeduction`), `handleFile`, `processImport`, `finaliseImport`, `bulkClassify`, `exportCSV`, `filteredTrips`, `monthlyData`/`maxMon`, and the entire `{tab === "travel" && (...)}` JSX.
- Move `toast` state, the toast `useEffect`, and `notify` into this component — grep confirms `notify(` is only ever called from Travel/Logbook functions (`saveTrip`, `handleFile`, `processImport`, `finaliseImport`). Do NOT leave them in the shell (research Pitfall 5). The toast render markup (the `fixed right-5 top-5` block) and the hidden `<input ref={fileRef} type="file" ...>` also move into this component — the only `fileRef.current?.click()` trigger (the "Upload CSV" button) is inside the Travel JSX, so nothing outside needs the input.
- `handleFile` currently calls `setTab("travel")`; drop that call — the component IS the travel tab, and it is always mounted. No `setTab`/`tab` reference should remain in the extracted component.
- Read `getDeemedRate` from `calc-helpers` and `rulePack` via `useRulePack()`. Publish `travelDeduction` via `useSummaryWriter()`; do not read `useSummary()`.
- Wrap `tripStats`, `filteredTrips`, `monthlyData` (and `maxMon`) in `useMemo` keyed on `trips` (+ `filterMonth` for `filteredTrips`) — behaviour identical, just not recomputed on unrelated state changes within the tab.
- Colocated `useState`; remove ALL travel/toast state and handlers from the shell.
- Always-mounted + CSS-hidden wrapper.
- Build gate `npm run build` (Turbopack); no `--webpack`.
</constraints>

<interfaces>
Source anchors (current shell):
- Travel state: lines ~179-196 (`trips`, `tripForm`, `editId`, `filterMonth`, `vehicleValue`, `vehicleName`, `uploadData`, `colMap`, `importTrips`, `uploadStep`, `fileRef`).
- Toast: state ~173-176, effect ~272-282 (`notify`).
- Trip helpers/calc: ~284-382 (allocTrip/newTrip/saveTrip/changeTripType/tripStats/bizPct/deemed-cost).
- Upload/import/export: ~541-643 (handleFile/processImport/finaliseImport/bulkClassify/exportCSV/filteredTrips/monthlyData/maxMon).
- Toast + file input JSX: ~658-674. Travel JSX: `{tab === "travel" && (...)}` ~772-1429.

```tsx
"use client";
import { useState, useEffect, useRef, useMemo } from "react";
import { getDeemedRate } from "@/components/individual-tax/tax-tools/calc-helpers";
import { useRulePack } from "@/components/individual-tax/tax-tools/rulepack-context";
import { useSummaryWriter } from "@/components/individual-tax/tax-tools/summary-context";
import { /* StatCard, ResultCard, Field, fmt, fmtKm, pct, MONTHS, inputCls, selectCls, type Trip, type UploadData */ } from "@/components/individual-tax/tax-tools/shared";
```
Summary key already defined: `travelDeduction`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TravelLogbookTab with all travel state, logic, toast, and JSX</name>
  <files>src/components/individual-tax/tax-tools/travel-logbook-tab.tsx</files>
  <action>
Create `travel-logbook-tab.tsx` (`"use client"`). Move VERBATIM (per anchors above): all 10 travel state slots + `fileRef`; the `toast` state, its `useEffect`, and `notify`; `allocTrip`/`newTrip`/`saveTrip`/`changeTripType`; the deemed-cost block (using `const { rulePack } = useRulePack();` and `getDeemedRate(rulePack, vVal)`); `handleFile` (WITHOUT the `setTab("travel")` line), `processImport`, `finaliseImport`, `bulkClassify`, `exportCSV`; and the full Travel JSX plus the toast markup and hidden file `<input ref={fileRef} .../>`. Wrap `tripStats`, `filteredTrips`, `monthlyData`/`maxMon` in `useMemo`. Add `useSummaryWriter()` + `useEffect` publishing `travelDeduction` to `"travelDeduction"`. Import needed pieces from `shared`/`calc-helpers`. Confirm no `tab`/`setTab` reference remains.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit</automated>
  </verify>
  <done>TravelLogbookTab exists: all travel state/logic/toast/file-input/JSX moved, deemed-cost via useRulePack, aggregates memoized, publishes travelDeduction, no tab reference; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TravelLogbookTab into the shell and strip the shell to a thin container</name>
  <files>src/components/individual-tax/tax-tools.tsx</files>
  <action>
Shell edits (`TaxToolsInner`): delete all travel state (the 10 slots + `fileRef`), toast state/effect/`notify`, all trip helpers and calc (`allocTrip`..`travelDeduction`), the upload/import/export functions, `filteredTrips`, `monthlyData`/`maxMon`, the shell's `travelDeduction` `useEffect` publish, the top-level toast markup, and the top-level hidden file `<input>`. Replace the `{tab === "travel" && (...)}` block with `<div className={tab === "travel" ? "" : "hidden"}><TravelLogbookTab /></div>`. Add the import.
After this, `TaxToolsInner` should contain only: `const [tab, setTab] = useState<TabKey>(...)`, `const { assessmentYear, setAssessmentYear } = useRulePack()` (or just what the nav/select needs), the `NAV` array, the nav bar + year `<select>`, and the 8 always-mounted CSS-hidden `<div className={tab === "x" ? "" : "hidden"}><XTab/></div>` wrappers. Confirm no calculator state or math remains in the shell and all 8 tabs use the hidden-wrapper pattern (no remaining `{tab === "x" && (...)}` for tab content).
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Shell renders all 8 tabs via hidden-mounted wrappers; no calculator/travel/toast state or math remains inline; tsc + full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Final render-isolation coverage and full phase verification</name>
  <files>src/components/individual-tax/tax-tools/render-isolation.test.tsx</files>
  <action>
Extend `render-isolation.test.tsx` with a Travel-vs-other-calculator isolation test: render `TravelLogbookTab` alongside e.g. `MedicalTab` under `<RulePackProvider><TaxToolsSummaryProvider>`, each in its own `<Profiler>`; type into the Travel vehicle-value input; assert the Medical profiler `not.toHaveBeenCalled()`. Confirm the test file now covers cross-isolation across the calculator set and the tab-switch persistence test still passes. Then run the full phase gate.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <done>Travel isolation test passes; full suite + Turbopack build green; PERF-01 satisfied end-to-end.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean; `npm test` passes (incl. all render-isolation tests); `npm run build` (Turbopack) succeeds.
- Grep the shell: no `trips`/`tripForm`/`toast`/`calcMedical`/`calcCGT`/etc. or `medResult`/`travelDeduction` computations remain; only `tab`/`NAV`/nav/`select`/8 tab wrappers.
- Grep the shell: no `{tab === "..." && (` for tab CONTENT — every tab is an always-mounted CSS-hidden wrapper.
- Manual/logical: all 8 tabs render and compute identically to before the phase; Dashboard shows all five live totals; year switch propagates; toast still appears on trip actions; input persists across tab switches.
</verification>

<success_criteria>
- All 8 calculators are standalone components with colocated local state (criterion 1 complete).
- Typing in any one calculator does not re-render the other input calculators (criterion 2 — Profiler-proven across the set; Dashboard is the documented exception).
- All rulepack-dependent calculators read rates via `useRulePack()` (criterion 3 complete).
- Shell is thin; behaviour identical (full suite + build green).
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-decomposition/05-06-SUMMARY.md`.
</output>

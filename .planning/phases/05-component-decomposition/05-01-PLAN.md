---
phase: 05-component-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/individual-tax/tax-tools.tsx
  - src/components/individual-tax/tax-tools/shared.tsx
  - src/components/individual-tax/tax-tools/calc-helpers.ts
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "The app builds and all existing tests pass with zero behaviour change after the move"
    - "Presentational UI pieces, formatters, and pure tax-bracket helpers live in their own files, imported by the still-single TaxTools"
  artifacts:
    - path: "src/components/individual-tax/tax-tools/shared.tsx"
      provides: "StatCard, ResultCard, Highlight, Field, inputCls, selectCls, fmt, fmtKm, pct, MONTHS, Trip, UploadData, TabKey"
      min_lines: 120
    - path: "src/components/individual-tax/tax-tools/calc-helpers.ts"
      provides: "calcTax, getMarginalRate, getDeemedRate (relocated verbatim, rulePack-parameterized)"
      min_lines: 25
  key_links:
    - from: "src/components/individual-tax/tax-tools.tsx"
      to: "src/components/individual-tax/tax-tools/shared.tsx"
      via: "named imports"
      pattern: "from \"@/components/individual-tax/tax-tools/shared\""
    - from: "src/components/individual-tax/tax-tools.tsx"
      to: "src/components/individual-tax/tax-tools/calc-helpers.ts"
      via: "named imports"
      pattern: "from \"@/components/individual-tax/tax-tools/calc-helpers\""
---

<objective>
Scaffold the `tax-tools/` subfolder with a behaviour-neutral file move: relocate the stateless presentational components, format helpers, shared constants/types, and the three pure tax-bracket helper functions out of the 2,156-line `tax-tools.tsx` monolith into `shared.tsx` and `calc-helpers.ts`, then import them back. No component structure changes, no calculator math changes — this is a pure extraction that keeps the monolith working identically while creating the shared modules every subsequent extraction plan will import from.

Purpose: Establishes the folder and the shared surface (interface-first) so calculator extractions in later waves import proven, already-in-place helpers instead of each re-deriving or re-copying them. Closes the "two copies of the bracket formula" drift risk at the code level by giving calculators a single `calcTax`/`getMarginalRate`/`getDeemedRate` module.
Output: `tax-tools/shared.tsx`, `tax-tools/calc-helpers.ts`, and a `tax-tools.tsx` that imports from both and behaves identically.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-component-decomposition/05-RESEARCH.md
@src/components/individual-tax/tax-tools.tsx

<constraints>
- This is a REFACTOR. Behaviour must be preserved EXACTLY. The only observable diff is which file code lives in.
- `tax-tools.tsx` MUST keep `export function TaxTools()` as a NAMED export — its sole consumer `src/app/(protected)/individual-tax/tools/page.tsx` does `import { TaxTools } from "@/components/individual-tax/tax-tools"`. Do NOT convert to default export.
- Relocate `calcTax`/`getMarginalRate`/`getDeemedRate` VERBATIM (research Open Question 2 — decision: relocate-only, do NOT consolidate with `calculation-service.ts`'s private `getBracketTax`; that cross-module merge is explicitly out of scope for this phase).
- New files use the `@/*` path alias (resolves to `./src/*`) for any imports, per repo convention.
- Build gate is `npm run build` (Next 16 Turbopack). Do NOT run `next build --webpack` — it hits a pre-existing, unrelated Estates route-export error documented in `.planning/phases/04-import-pipeline/deferred-items.md`, out of scope here.
</constraints>

<interfaces>
<!-- Current module-level code in tax-tools.tsx (lines 1-163) to be relocated. Executor moves these verbatim. -->

calc-helpers.ts must export (currently tax-tools.tsx lines 10-28):
```ts
import type { IndividualTaxRulePack } from "@/modules/individual-tax/types";

// returns an element of rulePack.travelDeemedCostTable
type DeemedCostRow = IndividualTaxRulePack["travelDeemedCostTable"][number];

export function calcTax(rulePack: IndividualTaxRulePack, taxable: number): number;
export function getMarginalRate(rulePack: IndividualTaxRulePack, taxable: number): number;
export function getDeemedRate(rulePack: IndividualTaxRulePack, v: number): DeemedCostRow;
```

shared.tsx must export (currently tax-tools.tsx lines 29-163, plus `TabKey`):
- Values: `fmt`, `fmtKm`, `pct`, `MONTHS`, `inputCls`, `selectCls`
- Components: `StatCard`, `ResultCard`, `Highlight`, `Field`
- Types: `Trip`, `UploadData`, `TabKey`
(`TabKey` is moved to `shared.tsx` because it is a genuinely shared type — the shell, the nav, and `DashboardTab`'s `onNavigate` prop all reference it. The `NAV` array and the `tab` state itself stay in the shell.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract pure tax-bracket helpers into calc-helpers.ts</name>
  <files>src/components/individual-tax/tax-tools/calc-helpers.ts</files>
  <action>
Create `src/components/individual-tax/tax-tools/calc-helpers.ts`. Move the three arrow-function helpers `calcTax`, `getMarginalRate`, `getDeemedRate` (currently `tax-tools.tsx` lines 10-28) VERBATIM, converting each to an exported function (keep identical bodies — the SARS bracket formula `b.baseTax + (taxable - b.min + 1) * b.rate`, the `<= 0 → 0.18` / fallback `0.45` marginal-rate branches, and the deemed-rate `.find(... ?? table[0])` must be byte-for-byte the same logic).
Import `type { IndividualTaxRulePack }` from `@/modules/individual-tax/types`. Add `type DeemedCostRow = IndividualTaxRulePack["travelDeemedCostTable"][number]` and annotate `getDeemedRate`'s return as `DeemedCostRow`. This is NOT a `"use client"` file (pure functions, no React).
Do NOT merge with `calculation-service.ts` — relocate only.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit</automated>
  </verify>
  <done>calc-helpers.ts exists exporting the three functions with identical logic; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract presentational components, formatters, constants, and types into shared.tsx</name>
  <files>src/components/individual-tax/tax-tools/shared.tsx</files>
  <action>
Create `src/components/individual-tax/tax-tools/shared.tsx` with `"use client"` at the top. Move VERBATIM from `tax-tools.tsx` (lines 29-163): the format helpers `fmt`, `fmtKm`, `pct`; the `MONTHS` array; the `TabKey`, `Trip`, and `UploadData` type definitions; the `StatCard`, `ResultCard`, `Highlight`, `Field` components; and the `inputCls`/`selectCls` class-string constants. Export all of them (named exports). Keep `React.ReactNode` typing on `Field`'s children exactly as-is.
Leave the `NAV` array and the `tab` state in `tax-tools.tsx` (shell-owned).
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit</automated>
  </verify>
  <done>shared.tsx exists exporting all listed values, components, and types; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire tax-tools.tsx to import from the new modules and delete the relocated code</name>
  <files>src/components/individual-tax/tax-tools.tsx</files>
  <action>
In `tax-tools.tsx`: delete the now-relocated definitions (the three helpers, formatters, `MONTHS`, `TabKey`/`Trip`/`UploadData`, the four presentational components, `inputCls`/`selectCls`). Keep the `NAV` array. Add two import lines:
`import { calcTax, getMarginalRate, getDeemedRate } from "@/components/individual-tax/tax-tools/calc-helpers";`
`import { StatCard, ResultCard, Highlight, Field, inputCls, selectCls, fmt, fmtKm, pct, MONTHS, type Trip, type UploadData, type TabKey } from "@/components/individual-tax/tax-tools/shared";`
Preserve the existing `"use client"`, the `useState/useEffect/useRef/useCallback` React import, the rulepack-registry import, and the named `export function TaxTools()`. Every call site inside `TaxTools` (e.g. `calcTax(rulePack, ...)`, `<StatCard .../>`, `fmt(...)`) stays unchanged — they now resolve to the imports. Confirm no stray duplicate declarations remain.
  </action>
  <verify>
    <automated>cd C:/Users/HP/sa-tax-platform && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>tax-tools.tsx imports helpers/shared from the new files, no duplicate local definitions; tsc clean; full test suite passes unchanged.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `npm test` — entire existing suite passes (behaviour unchanged).
- `npm run build` succeeds (Turbopack). Note: if a pre-existing Estates route-export error surfaces, confirm it is the one documented in `.planning/phases/04-import-pipeline/deferred-items.md` and unrelated to this change — do not fix it here.
- `src/app/(protected)/individual-tax/tools/page.tsx`'s `import { TaxTools }` still resolves (named export preserved).
</verification>

<success_criteria>
- `tax-tools/shared.tsx` and `tax-tools/calc-helpers.ts` exist and are the single home for the relocated code.
- `tax-tools.tsx` no longer defines those pieces locally and imports them instead.
- Zero behaviour change: existing tests + build green; the page still renders the same UI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-decomposition/05-01-SUMMARY.md`.
</output>
